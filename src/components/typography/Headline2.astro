---
import { cn } from "@/ts/utils";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"h2">;

const { class: className, ...rest } = Astro.props;
---

<h2 class={cn("text-2xl font-bold md:text-3xl", className)} {...rest}>
	<slot />
</h2>
